{
  "name": "@jupyter/ydoc-top-repo",
  "version": "0.0.1",
  "private": true,
  "files": [],
  "workspaces": [
    "javascript",
    "tests"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run build:test && lerna run test"
  },
  "devDependencies": {
    "lerna": "^6.3.0"
  }
}
