version: 2

build:
  os: ubuntu-20.04
  tools:
    python: "3.10"
    nodejs: "14"
  jobs:
    install:
      - pip install --upgrade pip
      - pip install --group 'docs'

sphinx:
  configuration: docs/source/conf.py
